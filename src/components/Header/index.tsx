import { HeaderContainer, HeaderWrapper } from "./styles";

export const Header = () => {
  return (
    <HeaderContainer>
      <HeaderWrapper>
        <img src="/logo.svg" alt="logo" />
      </HeaderWrapper>
    </HeaderContainer>
  );
};
